deps = [
  dependency('x11'),
  dependency('xft'),
  dependency('xext'),
]

sources = [ 'main.c', 'sort.c', 'ui.c' ]

bin = executable(
  'xmenu',
  sources,
  dependencies : deps,
)

# test('run', bin)
